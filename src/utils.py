"""Shared helpers (logging setup, distance lookups, id generation, etc.)."""

import logging


def get_logger(name: str, log_dir: str = "outputs/logs") -> logging.Logger:
    raise NotImplementedError
